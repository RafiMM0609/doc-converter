# PDF Converter - Visual Guide & Architecture

## 🎨 User Interface Layout

```
┌─────────────────────────────────────────────────────────┐
│                                                           │
│                      📄 PDF CONVERTER                    │
│            Convert & Merge your PDF files with ease      │
│                                                           │
├─────────────────────────────────────────────────────────┤
│  [Convert to Image]  [Merge PDFs]     ← Tab Navigation   │
├─────────────────────────────────────────────────────────┤
│                                                           │
│  Convert PDF to JPG                                      │
│  Upload a PDF file and convert its first page to JPG    │
│                                                           │
│  ┌───────────────────────────────────────────────────┐  │
│  │               📎 Drop PDF here                     │  │
│  │                                                    │  │
│  │          or click to select                        │  │
│  │      Maximum file size: 10 MB                      │  │
│  └───────────────────────────────────────────────────┘  │
│                                                           │
│  [Convert to JPG]  ← Action Button                       │
│                                                           │
├─────────────────────────────────────────────────────────┤
│                                                           │
│  Progress Indicator (during conversion):                 │
│  ████████████░░░░░░░  Converting... Please wait          │
│                                                           │
├─────────────────────────────────────────────────────────┤
│                                                           │
│  Result Display (after conversion):                      │
│                                                           │
│  ✓ Conversion Successful!                               │
│  ┌─────────────────────────┐                            │
│  │                         │                            │
│  │   [Preview JPG Image]   │                            │
│  │                         │                            │
│  └─────────────────────────┘                            │
│  File size: 245 KB • Format: JPG                        │
│  [Download JPG]  [Convert Another]                      │
│                                                           │
├─────────────────────────────────────────────────────────┤
│                                                           │
│  PDF Converter API v1.0.0 • View API Docs               │
│                                                           │
└─────────────────────────────────────────────────────────┘
```

## 🔄 Application Data Flow

```
┌──────────────┐
│  User Action │
└────────┬─────┘
         │
         v
┌────────────────────────┐
│  UI Event Handler      │
│ (JavaScript)           │
├────────────────────────┤
│ • File validation      │
│ • File preview         │
│ • UI state management  │
└────────────┬───────────┘
             │
             v
      ┌──────────────────┐
      │  FormData API    │
      │ (File encoding)  │
      └────────┬─────────┘
               │
               v
      ┌──────────────────┐      ┌──────────────────┐
      │  Fetch API       │─────>│  FastAPI Server  │
      │ (HTTP Request)   │      │ (Port 8000)      │
      └──────────────────┘      └────────┬─────────┘
                                         │
                                         v
                        ┌─────────────────────────────┐
                        │  API Router (/api/...)      │
                        │                             │
                        │ convert-pdf-to-jpg          │
                        │ merge-pdfs                  │
                        └────────────┬────────────────┘
                                     │
                                     v
                        ┌─────────────────────────────┐
                        │  PDF Service                │
                        │  (Business Logic)           │
                        │                             │
                        │ • PDF to Image conversion   │
                        │ • PDF merging               │
                        └────────────┬────────────────┘
                                     │
                                     v
                        ┌─────────────────────────────┐
                        │  File Processing            │
                        │                             │
                        │ • pdf2image (PDF->JPG)      │
                        │ • PyPDF2 (Merge)            │
                        │ • poppler-utils (PDF lib)   │
                        └────────────┬────────────────┘
                                     │
                                     v
                        ┌─────────────────────────────┐
                        │  Response Blob              │
                        │                             │
                        │ • JPG image bytes           │
                        │ • Merged PDF bytes          │
                        └─────────────┬───────────────┘
                                      │
                                      v
      ┌──────────────────┐      ┌──────────────────┐
      │  Blob Handler    │<─────│  HTTP Response   │
      │ (JavaScript)     │      └──────────────────┘
      └────────┬─────────┘
               │
               v
      ┌──────────────────┐
      │  Display Result  │
      │                  │
      │ • Show preview   │
      │ • Enable download│
      │ • User feedback  │
      └────────┬─────────┘
               │
               v
      ┌──────────────────┐
      │  Download File   │
      │  (User Action)   │
      └──────────────────┘
```

## 🏗️ Architecture Diagram

```
┌─────────────────────────────────────────────────────────────┐
│                     WEB BROWSER                             │
│ ┌──────────────────────────────────────────────────────────┐│
│ │              index.html (UI)                             ││
│ ├──────────────────────────────────────────────────────────┤│
│ │  ┌────────────────┐  ┌──────────────┐  ┌─────────────┐  ││
│ │  │  DOM Elements  │  │  CSS Styles  │  │  JavaScript │  ││
│ │  │  (Tabs, Forms) │  │  (Responsive)│  │  (Logic)    │  ││
│ │  └────────┬───────┘  └──────────────┘  └──────┬──────┘  ││
│ │           │                                    │           ││
│ │           │────────────────┬───────────────────┘           ││
│ │                            │                               ││
│ │                      ┌─────v──────┐                        ││
│ │                      │  app.js     │                        ││
│ │                      │  - Events   │                        ││
│ │                      │  - Fetch    │                        ││
│ │                      │  - Handlers │                        ││
│ │                      └─────┬───────┘                        ││
│ └────────────────────────────┼──────────────────────────────┘│
└──────────────────────────────┼────────────────────────────────┘
                               │
                        ┌──────v───────┐
                        │  HTTP/CORS   │
                        │  (Port 8000) │
                        └──────┬───────┘
                               │
┌──────────────────────────────┼────────────────────────────────┐
│                     FASTAPI SERVER                            │
│                                                                │
│  ┌────────────────────────────────────────────────────────┐  │
│  │  main.py                                               │  │
│  │  - CORS Middleware                                     │  │
│  │  - Static file mounting (/index.html, /static/*)       │  │
│  │  - Router initialization                              │  │
│  └────────────────────────────────────────────────────────┘  │
│                               │                               │
│  ┌────────────────────────────┼───────────────────────────┐  │
│  │  routers/converter.py                                  │  │
│  │  ├── POST /api/convert-pdf-to-jpg                      │  │
│  │  └── POST /api/merge-pdfs                              │  │
│  └────────────────────────────┼───────────────────────────┘  │
│                               │                               │
│  ┌────────────────────────────┼───────────────────────────┐  │
│  │  services/pdf_service.py                               │  │
│  │  ├── convert_pdf_to_jpg()                              │  │
│  │  └── merge_pdfs()                                      │  │
│  └────────────────────────────┼───────────────────────────┘  │
│                               │                               │
│  ┌────────────────────────────┼───────────────────────────┐  │
│  │  utils/file_handler.py                                 │  │
│  │  ├── validate_file()                                   │  │
│  │  ├── save_upload_file()                                │  │
│  │  ├── cleanup_file()                                    │  │
│  │  └── get_file_size()                                   │  │
│  └────────────────────────────┼───────────────────────────┘  │
│                               │                               │
│  ┌────────────────────────────┼───────────────────────────┐  │
│  │  External Libraries                                    │  │
│  │  ├── pdf2image (pdf2image library)                     │  │
│  │  ├── PyPDF2 (PDF merger)                               │  │
│  │  └── poppler-utils (PDF processor)                     │  │
│  └────────────────────────────┼───────────────────────────┘  │
│                               │                               │
│                          ┌────v─────┐                        │
│                          │  Output   │                        │
│                          │  - JPG    │                        │
│                          │  - PDF    │                        │
│                          └──────────┘                        │
│                                                                │
└─────────────────────────────────────────────────────────────┘
```

## 📊 Feature Flow - Convert PDF to JPG

```
START
  │
  ├─> User uploads PDF
  │   └─> File validation (type, size)
  │       ✓ Valid? Continue : ✗ Show error
  │
  ├─> Display file preview
  │   └─> Show filename & size
  │
  ├─> User clicks "Convert to JPG"
  │   └─> POST /api/convert-pdf-to-jpg
  │
  ├─> Processing...
  │   └─> Show progress bar
  │
  ├─> Response received
  │   ├─> Create Blob object
  │   └─> Hide progress bar
  │
  ├─> Display result
  │   ├─> Show image preview
  │   ├─> Display file info (size)
  │   └─> Enable download button
  │
  ├─> User clicks "Download JPG"
  │   └─> File saved to Downloads
  │
  └─> END (or Convert Another)
```

## 📊 Feature Flow - Merge PDFs

```
START
  │
  ├─> User selects multiple PDFs
  │   ├─> File validation (all must be PDF)
  │   ├─> File validation (size limit)
  │   └─> ✓ Valid? Continue : ✗ Show error
  │
  ├─> Display file list
  │   ├─> Show all filenames
  │   ├─> Show file sizes
  │   ├─> Show count
  │   └─> Enable remove options
  │
  ├─> (Optional) User removes file(s)
  │   └─> Update file list
  │
  ├─> User clicks "Merge PDFs" (when 2+ files)
  │   └─> POST /api/merge-pdfs
  │
  ├─> Processing...
  │   └─> Show progress bar
  │
  ├─> Response received
  │   ├─> Create Blob object
  │   └─> Hide progress bar
  │
  ├─> Display result
  │   ├─> Show merge info (file count, size)
  │   └─> Enable download button
  │
  ├─> User clicks "Download PDF"
  │   └─> File saved to Downloads
  │
  └─> END (or Merge More)
```

## 🔄 User Interaction Sequence

```
Browser                          Server
  │                               │
  ├──── Load /index.html ────────>│
  │                               │
  │<── HTML + CSS + JS ───────────│
  │                               │
  ├── Load /static/style.css ────>│
  │<── CSS content ────────────────│
  │                               │
  ├── Load /static/app.js ───────>│
  │<── JavaScript ─────────────────│
  │                               │
  │ [User selects file and clicks Convert]
  │                               │
  ├─ POST /api/convert-pdf-to-jpg─>│
  │  (FormData with file)         │
  │                               ├─ Process PDF
  │                               ├─ Generate JPG
  │                               │
  │<─── Binary JPG Response ──────│
  │                               │
  │ [Display preview + download]  │
  │                               │
  │ [User clicks Download]        │
  │ (Browser handles download)    │
  │                               │
```

## 💾 File Operations Flow

```
User File Upload
       │
       v
JavaScript File Input
       │
       ├─ Read file metadata
       ├─ Validate type (.pdf)
       ├─ Validate size (<10MB)
       │
       v
FormData Encoding
       │
       ├─ Multipart/form-data
       ├─ Binary file content
       │
       v
Fetch API (HTTP POST)
       │
       v
Server File Handler
       │
       ├─ Save to temp directory
       ├─ Validate file integrity
       │
       v
PDF Processing
       │
       ├─ pdf2image → convert to JPG
       │  OR
       ├─ PyPDF2 → merge PDFs
       │
       v
Output Generation
       │
       ├─ Save result to temp
       ├─ Read as bytes
       │
       v
HTTP Response
       │
       ├─ Set correct MIME type
       ├─ Stream binary content
       │
       v
Cleanup
       │
       ├─ Delete temp input files
       ├─ Delete temp output (after download)
       │
       v
Browser Handling
       │
       ├─ Create Blob object
       ├─ Generate download link
       ├─ Trigger download
       ├─ Revoke object URL
       │
       v
User Downloads File
```

## 🎯 API Endpoints Summary

```
GET /
  └─ Returns API info & UI URL

GET /health
  └─ Returns service status

POST /api/convert-pdf-to-jpg
  ├─ Input: PDF file (multipart/form-data)
  ├─ Processing: Convert first page to JPG
  └─ Output: JPG image file

POST /api/merge-pdfs
  ├─ Input: Multiple PDF files (multipart/form-data)
  ├─ Processing: Merge all PDFs
  └─ Output: Merged PDF file

GET /index.html
  └─ Returns main UI page

GET /static/style.css
  └─ Returns application styles

GET /static/app.js
  └─ Returns application logic

GET /docs
  └─ Swagger API documentation

GET /redoc
  └─ ReDoc API documentation
```

---

This architecture ensures:
✅ Clean separation of concerns  
✅ Responsive user experience  
✅ Secure file handling  
✅ Efficient data transfer  
✅ Proper error handling  
✅ Complete documentation
